jbok {
  network {
    rpc-bind-address = {
      host = "127.0.0.1"
      port = 9999
      scheme = "ws"
    }

    peer-bind-address = {
      host = "127.0.0.1"
      port = 10000
      scheme = "tcp"
    }
  }
}